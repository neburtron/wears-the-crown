import src.commands as commands
import scripts.simple as simple

# This script being named after the domain is a temp thing. Scripts here are supposed
# to be more general, with specialized code in the domain. I want to start with the
# structure of scripts here run by domain scripts there though, so I am.


def agent():
    return
    
def event():
    return

